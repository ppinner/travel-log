spring:
  application:
    name: travel-log
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/travellog}

server:
  port: ${SERVER_PORT:8080}

jwt:
  secret: ${JWT_SECRET:dev-only-secret-change-me-dev-only-secret-change-me}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
